const readmePath = "README.md";
const chunkSize = 500;
const encodeFolder = "encoded";
const decodeFolder = "decoded";

module.exports = {
  readmePath,
  chunkSize,
  encodeFolder,
  decodeFolder,
};
