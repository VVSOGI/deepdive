const readmePath = "README.md";
const chunkSize = 500;
const encodeFolder = "just-chunk-encoded";
const decodeFolder = "just-chunk-decoded";

module.exports = {
  readmePath,
  chunkSize,
  encodeFolder,
  decodeFolder,
};
